# script location
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

WD=$PWD

# go into this location
cd $DIR

# to run it
java -cp "lib/xchart-3.6.4.jar:lib/mjson-1.4.1.jar:bin/IcarusStats.jar" IcarusStats $@

# go back to where script was executed from
cd $WD